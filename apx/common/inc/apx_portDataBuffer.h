#ifndef APX_PORT_DATA_BUF_H
#define APX_PORT_DATA_BUF_H

//////////////////////////////////////////////////////////////////////////////
// INCLUDES
//////////////////////////////////////////////////////////////////////////////
#include <stdint.h>


//////////////////////////////////////////////////////////////////////////////
// CONSTANTS AND DATA TYPES
//////////////////////////////////////////////////////////////////////////////
typedef struct apx_portDataBuffer_tag
{
   uint8_t *buf; //pointer to data
   uint32_t len; //length of the array
   uint8_t isAllocated; //true if buf contains allocated memory that needs to be freed later
}apx_portDataBuffer_t;

#define MAX_PORT_DATA_LEN 1024u*1024u*10u

//////////////////////////////////////////////////////////////////////////////
// GLOBAL VARIABLES
//////////////////////////////////////////////////////////////////////////////


//////////////////////////////////////////////////////////////////////////////
// GLOBAL FUNCTION PROTOTYPES
//////////////////////////////////////////////////////////////////////////////
int8_t apx_portDataBuffer_create(apx_portDataBuffer_t *self, uint8_t *buf, uint32_t len);
void apx_portDataBuffer_destroy(apx_portDataBuffer_t *self);


#endif //APX_PORT_DATA_BUF_H
